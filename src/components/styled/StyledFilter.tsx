import styled, { css } from 'styled-components'

type Props = {
   theme?: {}
}

export const StyledFilter = styled.select<Props>`
   width: 15%;
`

export const StyledOption = styled.option<Props>``
